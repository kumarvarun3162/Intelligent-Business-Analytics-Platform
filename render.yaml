# render.yaml — place in project ROOT (ibap/), not inside backend/

services:
  - type: web
    name: ibap
    runtime: python
    rootDir: backend
    buildCommand: |
      apt-get install -y libpango-1.0-0 libpangoft2-1.0-0 libgdk-pixbuf2.0-0 libcairo2 libffi-dev || true
      pip install -r requirements.txt
      cd ../frontend
      npm install
      npm run build
      cd ../backend
      mkdir -p static
      cp -r ../frontend/dist/. static/
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
      - key: DEBUG
        value: "false"
      - key: RENDER_DISK_PATH
        value: /data
      - key: ALLOWED_ORIGINS
        value: https://ibap.onrender.com
      - key: GROQ_API_KEY
        sync: false
      - key: GROQ_MODEL
        value: llama-3.3-70b-versatile
    disk:
      name: ibap-data
      mountPath: /data
      sizeGB: 1
    healthCheckPath: /health